using UnityEngine;
using Core;

public class PlayerActions : MonoBehaviour
{
    /*
    public int DrillCooldown = 4;

    public bool DrillEquipped
    {
        get;
        private set;
    }

    private int currentDrillCooldown;

    void Update ()
    {
        currentDrillCooldown++;

        if (Controls.DrillKeyDown)
        {
            DrillEquipped = !DrillEquipped;
        }

        if (currentDrillCooldown >= DrillCooldown)
        {
            currentDrillCooldown = 0;


        }
    }
    */
}
